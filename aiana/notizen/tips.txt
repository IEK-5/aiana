Michael 3.3.22

- dev branch
- wenn mal mehr user: https://pypi.org/project/python-configuration/

poetry work flow:
    poetry add packagename oder toml datei editieren
    poetry lock (und dann auch pushen, wenn sie funktioniert)


    poetry install  (venv einrichten)

    https://python-poetry.org/docs/dependency-specification/


    poetry run
    poetry build (erstellt wheel datei um package für pip publizieren zu können)




-------

allgemein bei bugs: interactive window schließen um code neu zu laden

-----

bei df filtern und dem error:
ValueError: The truth value of a Series is ambiguous. Use a.empty, a.bool(), a.item(), a.any() or a.all()

- schon mit "&" bzw. "|" statt "and" bzw. "or" probiert?
- wenn das nicht hilft: df.loc[:,col] statt df[col]


-----
(to get code lines count:
push to git (no spaces in folder names) and then put following command in bash console within apv subdirectory as working path:

git ls-files --exclude-standard -- ':!:**/*.[pjs][npv]g' ':!:_tests_and_stuff' ':!:system_studies/before_restructuring_feb22' ':!:*/*only_reference*' ':!:**/*.txt' ':!:**/*.csv' ':!:**/*.pyc' ':!:*__pycache__*' ':!:*__init__*' ':!:*.gitignore*' | xargs wc -l

)